[phases.setup]
nixPkgs = ["python311", "ffmpeg", "aria2"]

[phases.install]
cmds = ["pip install -r requirements.txt", "pip install -U yt-dlp"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --worker-class gthread --threads 16 --timeout 360 --graceful-timeout 30"
